# Slim Airflow worker deps — only what runs inside the worker (cleaning + loading).
# ML and scraping deps live in their own container images under containers/.

# Preprocessing (cleaning stage runs in the worker — dedup, normalize, filter only)
langdetect==1.0.9

# Config
pyyaml==6.0.2

# Database migrations
alembic==1.13.2
sqlalchemy==2.0.32
pgvector==0.3.2

# Airflow providers
apache-airflow-providers-postgres==5.12.0
apache-airflow-providers-docker==3.14.0

# Testing
pytest==8.3.2
